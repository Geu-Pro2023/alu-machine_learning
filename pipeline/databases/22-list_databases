// List databases (sorted names only)
const conn = new Mongo();
conn.getDB("admin").adminCommand({ listDatabases: 1, nameOnly: true })
    .databases.map(d => d.name)
    .sort()
    .forEach(name => print(name));
quit();
